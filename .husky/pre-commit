#!/usr/bin/env bash

echo "🧹 Running pre-commit checks..."

# Ejecutar lint-staged con el entorno correcto
npx --no-install lint-staged
RESULT=$?

if [ $RESULT -ne 0 ]; then
  echo "❌ Lint-staged failed. Commit aborted."
  exit 1
fi

echo "✅ All checks passed. Proceeding with commit."
exit 0
