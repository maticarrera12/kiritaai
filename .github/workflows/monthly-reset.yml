name: Monthly Credit Reset

on:
  schedule:
    - cron: "0 0 1 * *"
  workflow_dispatch: 

jobs:
  reset-credits:
    runs-on: ubuntu-latest
    steps:
      - name: Call Monthly Reset Endpoint
        run: |
          curl -X GET "${{ secrets.NEXT_PUBLIC_APP_URL }}/api/cron/monthly-reset" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
